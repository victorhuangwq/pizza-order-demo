{
  "name": "checkers-pizza-demo",
  "version": "1.0.0",
  "description": "Checkers Pizza - demo pizza ordering site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "serve": "npx http-server -p 3000",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
